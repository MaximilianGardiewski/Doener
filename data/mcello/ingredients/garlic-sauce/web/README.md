# Web delivery

No tracked web derivative is stored here. The existing Mcello development server and preview build read the canonical master through `garlic-sauce.asset.json` and expose it at `/media/ingredients/ingredient.sauce.garlic.ribbon.png`.

The repository currently has no governed WebP conversion step, so no new converter or browser dependency was introduced.

